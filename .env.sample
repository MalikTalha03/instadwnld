# Copy to .env and adjust values, then source it locally (optional)
DJANGO_SECRET_KEY=change-me
DEBUG=False
# SQLite in Lambda is ephemeral; use RDS/Aurora or another external DB for production:
# DATABASE_URL=postgres://user:pass@host:5432/dbname
